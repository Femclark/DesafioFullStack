<div class="col-md-4 mx-auto">

    <div class="card">
        <div class="card-body">
            <form action="/carreras/edit/{{carreras.idCarrera}}" method="POST">
                <div class="form-group">
                    <input type="text" name="nombre" maxlength="30" placeholder="Nombre" class="form-control" value="{{carreras.nombre}}" autofocus>
                </div>
                <div class="form-group">
                    <button class="btn btn-success btn-block">
                        Guardar Carrera
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>